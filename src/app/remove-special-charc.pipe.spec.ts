import { RemoveSpecialCharcPipe } from './remove-special-charc.pipe';

describe('RemoveSpecialCharcPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSpecialCharcPipe();
    expect(pipe).toBeTruthy();
  });
});
